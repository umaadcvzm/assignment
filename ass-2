#include <stdio.h>
int main()
{
int a[10],i,n,c=0;
   printf("enter no of elements ");
   scanf("%d",&n); 
    printf("enter elements");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
   for(i = 0; i < n; i++) 
   {
       if(a[i] != 0)
       {
         a[c++] = arr[i];
       }
    }
   while(c < n)
 {
 a[c++] = 0;
   }
  printf("array:");
 for(i = 0; i < n; i++)
 {
       printf("  %d  ",a[i]);
   }
    return 0;
}
